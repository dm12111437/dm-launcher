# 《DM启动台》需求文档

> 版本：v0.3（含分组功能，待确认）
> 项目目录：`E:\startl\dm-launcher`

---

## 1. 项目概述

| 项目 | 内容 |
|---|---|
| 产品名称 | **DM启动台** |
| 目标 | Windows 桌面应用：自动扫描**多个游戏目录源**，将一级文件夹识别为游戏，一键启动 |
| 界面风格 | 参考 iOS 启动台（深色渐变壁纸、图标网格、悬停放大、名称标签） |
| 技术形态 | Electron v43（已安装），Node 生态 |
| 交付形式 | 先可运行版（`npm start`）；打包独立 exe 为阶段 3 |

### 1.1 跨机器运行说明（用户问题）
- 开发版：换机需装 Node.js + `npm install`；直接拷贝整个项目目录在同架构 Windows 下通常可用（不保证）。
- 打包版（阶段 3）：单个 exe，任意 Win10/11 双击即用，无需 Node。
- 用户配置（分组、覆盖设置、图标缓存）统一存 `%APPDATA%\dm-launcher\`，与程序安装位置无关。

---

## 2. 游戏目录源与分组（核心新增）

### 2.1 分组模型
**分组（Group）** = 名称 + 一个或多个目录路径 + 类型。游戏扫描以"分组"为单位：

```
分组 = {
  名称:  显示名（可自定义，平台组默认 Steam / WeGame / Epic）
  路径:  [目录1, 目录2, ...]   ← 一个分组可包含多个目录
  类型:  custom（自定义） | steam | wegame | epic
  启用:  是/否（关闭后该分组不扫描不显示）
}
```

- **默认分组「我的游戏」**：路径 `D:\Game`（原主目录），名称可改。
- 分组显示顺序 = 设置页中的排列顺序。

### 2.2 自定义目录导入
- **批量导入**：文件选择器一次可多选多个目录，一次性创建多个分组（每个目录默认独立成组、组名=目录名，可改）。
- **自定义添加**：可随时添加新目录；也可把目录追加进**已有分组**（一个组装多个目录，例如把不同盘的同类游戏归一组）。
- 删除分组、禁用分组、改名、增删路径，均在设置页操作。

### 2.3 平台自动扫描（Steam / WeGame / Epic）
自动探测各平台**默认安装位置**，探测到即自动建组并扫描；路径可手动修改。

| 平台 | 默认探测方式 | 游戏目录 |
|---|---|---|
| Steam | 注册表 `HKCU\Software\Valve\Steam\SteamPath` 读安装路径；再解析 `steamapps\libraryfolders.vdf` **获得全部库目录（含其他盘）** | `<库>\steamapps\common\*`（每个子文件夹=一个游戏） |
| WeGame | 常见路径探测（`C:\Program Files (x86)\WeGame` 等）+ 注册表尝试；找不到则建空组并提示手动设置路径 | `<WeGame>\rail_apps\<appid>\*` |
| Epic | 读取 `C:\ProgramData\Epic\EpicGamesLauncher\Data\Manifests\*.item`（JSON，含 InstallLocation）；兼容 `C:\Program Files\Epic Games\*` | 每个 InstallLocation 为一个游戏 |

- 平台组名默认 `Steam` / `WeGame` / `Epic`，**可自定义**。
- 用户现有 Epic 游戏已放在 `D:\Game`（归入默认分组）；平台自动扫描仍按各自默认目录执行，互不冲突，可手动增删路径。
- 游戏识别规则（杂质过滤 → 名称匹配 → 路径浅 → 名称短）对所有来源一致，见 §3。

---

## 3. 游戏识别规则（沿用 v0.2，简化、快速）

- 每个分组内：一级文件夹 = 一个游戏，递归找 `*.exe`（深度上限默认 4，可设）。
- 主程序自动选择：剔除杂质名单（installer/setup/redist/crash/uninstall/updater/downloader/webhelper/vc_redist/kernel/analyzer/benchmark/_be/launcher 等）→ exe 名与文件夹名匹配（忽略大小写、去版本号后缀）→ 路径浅者优先 → 名称短者优先；无弹窗。
- 无 exe 文件夹：显示但标灰"未找到启动程序"，可手动指定。
- 实测：D:\Game 16 个文件夹全量扫描 **153ms**。

---

## 4. 功能需求

### 4.1 主界面
- iOS 启动台风格图标网格；点击=启动；右键=游戏菜单。
- 顶部工具栏：搜索框、**排序下拉**、**分组开关**、刷新、设置、最小化、关闭（到托盘）。
- **分组开关**（新增）：
  - **开**：游戏按分组显示，每组上方显示**分组小标题**（如「我的游戏」「Steam」「Epic」），组内按当前排序方式排列，分组间按设置页顺序排列。
  - **关**：所有游戏混在一起，按当前排序方式统一排列。
- 状态栏：游戏总数 / 未设置启动程序数。

### 4.2 排序（沿用）
名称 A→Z / Z→A、修改时间 新→旧 / 旧→新、随机；记忆选择。

### 4.3 图标
从启动 exe 提取图标（PowerShell）缓存到应用数据目录；无图标用占位。

### 4.4 启动游戏
以 exe 目录为工作目录启动；启动后最小化到托盘、游戏退出自动恢复窗口；支持管理员启动（UAC）与启动参数（逐游戏记忆）。

### 4.5 右键菜单（每个游戏）
启动 / 打开文件夹 / 更改启动程序 / 恢复自动识别 / 以管理员身份启动 / 设置启动参数 / 重新扫描。

### 4.6 设置页
- **分组管理**（新增）：
  - 分组列表：名称（可改名）、类型徽标（自定义/Steam/WeGame/Epic）、路径列表（可增删）、启用开关、删除分组。
  - 「导入目录…」：多选目录批量导入（每个目录成一组，可改名）。
  - 「添加平台…」：一键探测 Steam / WeGame / Epic（按 §2.3），探测路径显示并可改。
  - 分组顺序调整（上移/下移）。
- 扫描深度、杂质名单编辑。
- 游戏目录列表（沿用）：全部游戏表格，每行可「打开路径」「更改程序」「恢复自动」。
- **样式修复**：所有下拉框（含展开选项列表）改为深色背景、白色文字，禁止白底。

### 4.7 本期不做
最近游玩记录、时长统计、封面下载（后续扩展）。

---

## 5. 数据存储（`%APPDATA%\dm-launcher\`）

- `settings.json`：
  ```json
  {
    "groups": [
      { "id": "g1", "name": "我的游戏", "type": "custom", "dirs": ["D:\\Game"], "enabled": true },
      { "id": "g2", "name": "Steam", "type": "steam", "dirs": ["C:\\Program Files (x86)\\Steam\\steamapps\\common"], "enabled": true }
    ],
    "scanDepth": 4,
    "sort": "name-asc",
    "grouped": true
  }
  ```
- `overrides.json`：逐游戏手动配置（**以游戏文件夹绝对路径为键**，避免跨分组重名冲突）：`{ "D:\\Game\\GTAV": { "exePath": "...", "admin": false, "args": "" } }`
- `icons\`：图标缓存（exe 路径 hash 命名）。
- 每次启动全量重扫；分组增删/路径变更后自动重扫对应分组。

---

## 6. 非功能需求与边界情况

- 启动快（实测 16 文件夹 84ms）、离线可用、单实例、兼容中文/空格/特殊字符路径。
- 平台目录探测不到（如未安装 WeGame / 库文件缺失）：不报错，自动隐藏，设置页可手动添加。
- Steam 多库（VDF 解析）、Epic manifest 缺失/损坏（跳过该条）、游戏在压缩包内未解压（标灰+手动指定）。
- 启动失败（exe 被删/权限）→ 弹窗提示并恢复窗口。

---

## 7. 里程碑

| 阶段 | 内容 | 状态 |
|---|---|---|
| 1 | Electron 骨架 + 扫描识别 + 网格列表 + 启动 + 托盘 | ✅ 已完成（已实测） |
| 2 | 图标 + 搜索 + 排序 + 右键菜单 + 设置页 + 持久化 | ✅ 已完成 |
| 3 | **分组功能**（多目录源、平台自动扫描、分组开关、分组管理 UI、下拉框样式修复） | ✅ 已完成（已实测） |
| 4 | 打包独立 exe（electron-builder） | ⏳ 后续 |

---

## 8. 已确认问题（已确认，开发已完成）

1. **批量导入多个目录**：每个目录独立成组（组名=目录名，可改）；支持后续把目录手动追加进已有分组。
2. **分组开关默认状态**：默认开启分组。
3. **平台探测不到时**：自动隐藏该平台分组，设置页可随时手动「+ 平台」重新探测或手动添加路径。
4. **分组顺序**：按设置页排列顺序（可上移/下移）。
5. 其余按 v0.3 文档执行；阶段 3 已完成并通过测试（16 游戏 84ms 扫描；VDF 解析、each 布局、禁用组、平台探测均验证；Epic 平台在本机实测探测到 4 个游戏）。

## 9. 迭代记录（开发备忘，供下次迭代直接上手）

- **去重策略（v0.5）**：扫描层按"组内去重、跨组允许重复"；主界面分组模式下按组展示（允许跨组重复），关闭分组平铺时前端按文件夹去重。默认分组只在设置文件无 `groups` 字段时创建（首次运行/旧版迁移），**用户删空分组后绝不自动复活**（`getSettings` 判断 `Array.isArray(s.groups)`）。
- **识别规则（v0.5）**：卸载类硬屏蔽（`unins|卸载`）；运行库文件夹名单 `RUNTIME_LIB_FOLDER`（含 Steamworks Shared）；辅助目录路径层级降权 `JUNK_SEG`；WeGame 专用 layout `wegame`（一级游戏文件夹 + rail_apps 子目录=游戏，屏蔽 common_apps/downloading/rail_user_data），探测优先含 rail_apps 的游戏根（注册表→客户端同级 WeGameApps→各盘根）。
- **联网图标（v0.5）**：Steam storesearch 按名搜 appid → header.jpg 存 `icons\<hash>-online.jpg`；优先 `net.fetch`（走系统代理），CDN 多镜像（cloudflare ×2 + akamai + pinyuncloud）；失败本会话不重试；每次扫描后自动补、设置页手动强制；结果/失败用顶部 toast（2 秒自动消失）。
- **窗口策略（v0.5）**：普通启动游戏时 `win.destroy()` 释放内存，游戏退出后重建窗口快速重扫（`restoreWindow`）；管理员启动无法跟踪退出，保持最小化到托盘 3 秒后恢复。
- **UI（v0.5）**：分组详情二级弹窗（目录/已排除/组内游戏）；顶部 toast 替代 alert；分组摘要行含"目录数·游戏数"；关于弹窗读取 `使用说明.md`（面向小白，无专业名词）。
- **v0.6**：无默认目录（`getSettings` 不再创建默认组，groups 非数组一律置 `[]`）；使用说明弹窗用内置 `mdToHtml`（标题/列表/加粗/转义）渲染；设置新增「🗑 清空缓存」（清 icons 缓存 + 内存失败集，`cache:clear` IPC）；联网图标改为**方形图标**（appdetails 的 `clienticon`/`icon` hash → steamcommunity public images，横幅 header 仅兜底），查找顺序 = 重命名名 → 文件夹名；分组详情表格新增「改名」按钮（弹窗宽度 920px、操作列 380px、名称列截断）。
- **v0.7（重要变更）**：**彻底取消联网图标**——删除全部网络图标代码（net.fetch/搜索/下载/手动按钮/toast），图标改为**纯本地解析**：① Steam 组：读 `appmanifest_*.acf` 的 name 匹配文件夹名 → appid → `steamapps\appcache\librarycache\{appid}_icon.{jpg,png}`；② 所有组：游戏文件夹内图标文件（`.ico` 优先，其次与文件夹同名/icon/folder 的 png）；③ 兜底：exe 图标提取缓存；每次扫描/刷新静默执行（`buildSteamIconMap` + `resolveLocalIcon`）。**新增大屏模式**：主界面「大屏」按钮 → 全屏 + 大卡片 UI（`body.bpm` CSS，隐藏普通标题栏、强制平铺去重列表）；手柄控制（`navigator.getGamepads` rAF 轮询：方向键/摇杆移动、A/X 启动、B/START/Esc 退出，首次连接不触发）；状态持久化 `settings.bigPicture`（游戏退出重建窗口后自动回大屏）；键盘方向键/回车/Esc 同样可用。
- **v0.8**：移除手动「清空缓存」功能（`cache:clear` IPC/按钮/说明全部删除）。修复分组模式下跨组重复游戏图标不加载：`onIcon` 处理器曾有 `break` 只更新第一张同文件夹卡片，改为更新全部匹配卡片；`scheduleIcons` 增加按 exe 去重（`done` Set），同一 exe 只提取一次、图标事件发一次即可让所有重复卡片刷新。
- **v0.9**：新增「🖼 重新识别图标」（`icons:reidentify` IPC：清空提取缓存 → 重新解析/提取）。**exe 图标升级为高清 256px**：`extractIconsBatch` 用 C# `IShellItemImageFactory`（SHCreateItemFromParsingName + GetImage 256，SIIGBF_BIGGERSIZEOK|ICONONLY）批量提取（一次 PowerShell 进程 + `-EncodedCommand` UTF-16LE），`Icon.ExtractAssociatedIcon` 32px 兜底；`findFolderIcon` 收紧：不再无脑取第一个 ico（避免 replays.ico 类误配），只接受名字含 icon 或与文件夹同名的 .ico。
- **v0.10（图标回归修复）**：批量脚本此前用 `'; '` 连接多行导致 `try {...}; catch {...}` 解析失败（PowerShell 不允许 try/catch 间有分号）→ 所有图标静默加载失败。修复：脚本改用 `\n` 连接（`buildIconBatchScript` 抽出可测试），实测 3/3 exe 含中文路径全部 256px 提取成功。**新增注册表已注册图标来源**（`buildRegisteredIconMap`：HKLM 64/32 + HKCU 的 Uninstall DisplayName/DisplayIcon，解析 `,N` 后缀、仅保留存在的图片文件，按名称模糊匹配），图标优先级：Steam librarycache → 文件夹图标 → 注册表图标 → exe 高清提取。图标相关纯逻辑全部移入 `scanner.js`（Node 可直接单测）。
- **v0.11（保底图标）**：全量实测 27/27 exe（D:\Game 15 + Steam 17）256px 提取成功；本机 Steam 无 `appcache\librarycache`（图标缓存仅 `steam\games\*.ico` 哈希文件，只能经注册表映射 16 个）→ exe 提取即保底方案。加固：① `resolveLocalIcon` 增加 `iconFileUsable`（存在且非空）校验，防损坏文件；② doScan 在无本地图标时**复用已提取缓存**（避免每次扫描重新提取、二次扫描立即显示）；③ 渲染端 `bindImgError`：图片加载失败自动回退占位图。
- **v0.12（保底强化 + 跨机器）**：①「🖼 重新识别图标」**不再删除任何缓存文件**（只重建注册表映射），保底图标永不丢失；② 提取链路双保险：批量 256px 失败项自动降级为逐游戏 `ExtractAssociatedIcon`（32px）重试，仍失败才 toast 提示（exe 无图标资源）；③ 跨机器适配：Steam 探测在注册表缺失时按**全盘符**（C-Z）探测常见安装位置 + 独立库目录（SteamLibrary/Steam Games）；WeGameApps 探测扩至全盘符；Steam 图标缓存兼容 `<库>\steam\appcache\librarycache` 布局；实测沙箱内（注册表被禁）仍能探测到 `D:\Steam\steamapps\common`。
- **v0.13（图标终态感知 + 改名修复）**：① 图标三态：有图标→img；有 exe 且提取中→**等待动画**（不再提前显示占位手柄）；**本来就没图标且提取彻底失败**（`iconFailed` 集合 + `icon-failed` 事件）→ 才显示 🎮；原导入图标（exe 提取缓存）任何操作都不清理，重新识别只重置失败标记；② 修复分组改名失效：`updateGroup` 返回的分组数组未回写 renderer 本地 `settings.groups`，导致设置页重绘显示旧名称（默认平台组同样受影响）——改名/启停开关改为回写返回值。
- **v0.14（彻底简化图标）**：按用户要求**取消全部高清图标逻辑**（`IShellItemImageFactory` 256px、Steam librarycache、文件夹图标、注册表 DisplayIcon、高清批量脚本全部删除），只保留**默认导入 exe 小图标**：`extractIconsSimple`（`ExtractAssociatedIcon` 32px，一次 PowerShell 进程批量，无 C# 编译最可靠）+ 失败逐游戏重试；图标三态收敛为两态（有图标→img，无→🎮），**移除转圈等待动画**；缓存复用、任何操作不清理；**默认平台分组（steam/wegame/epic）名称改为只读锁定**（`grp-name-static`），仅自定义分组可改名；跨机器盘符探测（Steam/WeGame 全盘符）保留。
- **v0.15（图标改用原生 API，删除全部脚本提取）**：用户反馈仍全部显示占位手柄 → **彻底删除 PowerShell 提取链路**（`ExtractAssociatedIcon`/`IShellItemImageFactory`/批量脚本/icon-ready、icon-failed 事件/scheduleIcons/sendIcon 全部移除）。改为 Electron 原生 **`app.getFileIcon(exe, {size:'large'})`**（就是资源管理器显示的 exe 图标，进程内同步获取、零外部依赖、无失败可能），**在 doScan 扫描时同步提取**并写缓存，游戏对象直接带 `iconPath`，渲染即图标、不再有 🎮 闪烁；`iconFailed` 集合仅标记确实无图标资源的 exe（下一次扫描不重复尝试）。
- **v0.16（通用图标检测 + CF 启动入口）**：用户反馈仍有蓝白通用图标（实测 `VALORANT.exe` 仅 19 色=通用图，`wp.exe` 59 色偏弱）→ 新增**通用图标检测**（`app.getFileIcon` → `getBitmap()` 像素采样颜色数 < 40 判定通用）→ 主 exe 为通用图时自动遍历同文件夹（深度≤2）其他 exe 取第一个真实图标做**显示图标**（启动程序不变，如无畏契约显示登录器图标）；兜底仍用主 exe 图标。**CF 修复**：WeGame 游戏根目录存在 `QQLogin.exe` 时优先作为启动入口（`makeGame` 内 wegame 类型特判），实测 CF → `QQLogin.exe` 正确。缓存键=主 exe 路径 hash，更换启动程序后自动重新解析。
- 测试：`node test-scan.js`（不依赖 Electron）；平台探测的注册表/网络调用在沙箱内会失败属正常。GUI 无法在沙箱内运行（命名管道被禁），需用户本机 `npm start` 验证。
